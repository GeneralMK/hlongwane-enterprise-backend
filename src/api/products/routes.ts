import Router from "@koa/router"; import {listProductsController,getProductController,createProductController,updateProductController,archiveProductController} from "./controllers.js"; import {requireAuthenticatedUser,requirePermission} from "../../middleware/permission.js";
const router=new Router({prefix:"/products"});router.get("/",listProductsController);router.get("/:id",getProductController);router.post("/",requireAuthenticatedUser,requirePermission("products.create"),createProductController);router.patch("/:id",requireAuthenticatedUser,requirePermission("products.update"),updateProductController);router.delete("/:id",requireAuthenticatedUser,requirePermission("products.delete"),archiveProductController);export default router;
