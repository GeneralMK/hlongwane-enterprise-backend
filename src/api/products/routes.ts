import Router from "@koa/router"; import {listProductsController,getProductController,createProductController,updateProductController,archiveProductController} from "./controllers.js"; import {requireAuthenticatedUser,requirePermission} from "../../middleware/permission.js";
const router=new Router({prefix:"/products"});router.get("/",listProductsController);router.get("/:id",getProductController);router.post("/",requireAuthenticatedUser,requirePermission("create:products"),createProductController);router.patch("/:id",requireAuthenticatedUser,requirePermission("update:products"),updateProductController);router.delete("/:id",requireAuthenticatedUser,requirePermission("delete:products"),archiveProductController);export default router;
