import Router from "@koa/router"; import {listProductVariantsController,getVariantController,createVariantController,updateVariantController} from "./controllers.js"; import {requireAuthenticatedUser,requirePermission} from "../../middleware/permission.js";
const router=new Router({prefix:"/product-variants"});router.get("/products/:productId",listProductVariantsController);router.get("/:id",getVariantController);router.post("/",requireAuthenticatedUser,requirePermission("create:products"),createVariantController);router.patch("/:id",requireAuthenticatedUser,requirePermission("update:products"),updateVariantController);export default router;
